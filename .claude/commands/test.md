Run the test suite for this project.

```bash
npm test
```

If a test name pattern is provided as `$ARGUMENTS`, run only matching tests:

```bash
node --test --test-name-pattern "$ARGUMENTS" test/**/*.test.js
```
